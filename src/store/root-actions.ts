// RootActions
// import { LocationChangeAction, RouterAction } from "react-router-redux";
// import { $call } from "utility-types";

// import { ACTION_HANDLERS } from "../routes/DashboardListing/modules";

// const returnsOfActions = [...Object.values(ACTION_HANDLERS)].map($call);

// type AppAction = typeof returnsOfActions[number];
// type ReactRouterAction = RouterAction | LocationChangeAction;

// export type RootAction = AppAction | ReactRouterAction;
